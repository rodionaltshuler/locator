<android.support.v4.widget.SlidingPaneLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/sp" >

    <!-- Left side pane. (Master) -->
    <FrameLayout android:id="@+id/contacts"
                 android:layout_width="match_parent"
                 android:layout_height="match_parent"
                 android:layout_gravity="left"
    />
    <!--<fragment android:id="@+id/contacts"
        android:name="com.ottamotta.locator.contacts.ui.TrustedContactsFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="left"  />
-->
    <!-- Right side page. (Slave)  -->
    <FrameLayout android:id="@+id/journal"
        android:layout_width="32dp"
        android:layout_height="match_parent"
        android:layout_gravity="right"
        android:layout_weight="1"
        />

</android.support.v4.widget.SlidingPaneLayout>